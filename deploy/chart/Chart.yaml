apiVersion: v2
name: ecr-ptc-webhook
description: Mutating admission webhook that rewrites container images to ECR
  pull-through cache paths.
type: application
version: 0.1.0
appVersion: "0.1.0"
kubeVersion: ">=1.27.0-0"
